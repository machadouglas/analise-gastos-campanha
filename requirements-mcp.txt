# Servidor MCP (src/mcp/). Instale junto do requirements.txt:
#   pip install -r requirements.txt -r requirements-mcp.txt
# SDK oficial 2.x (MCPServer), fixado por faixa compatível: revisão nova da
# especificação chega por bump deliberado aqui, autorizado pelos testes de
# protocolo. A 2.1 negocia a revisão 2026-07-28.
mcp~=2.1.1
# cliente HTTP do SDK 2.x; usado diretamente pelo teste de protocolo (em processo)
httpx2~=2.12.0
