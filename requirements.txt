# Versões travadas por faixa compatível (~=): um build novo não puxa major novo
# silenciosamente. Para subir de versão, mude aqui de propósito e rode os testes.
curl_cffi~=0.16.2
duckdb~=1.5.5
pandas~=3.0.5
tabulate~=0.10.0
pytest~=9.1.1
